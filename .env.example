# Neon database connection string (from Neon dashboard → Connection details)
# Copy to .env.local and set DATABASE_URL to enable saving data.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
